<Page x:Class="FSIncome.Windows.Pages.MainPagePages.HistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FSIncome.Windows.Pages.MainPagePages"
      mc:Ignorable="d" 
      Background="White"
      d:DesignHeight="500" d:DesignWidth="600"
      Title="HistoryPage">

    <DataGrid x:Name="dataGrid" Margin="10" AutoGenerateColumns="False" IsReadOnly="True" CanUserSortColumns="False" CanUserReorderColumns="False"
              CanUserResizeColumns="False" HorizontalScrollBarVisibility="Disabled" HeadersVisibility="Column" FontSize="14">
        <DataGrid.Columns>
            <DataGridTextColumn Header="ID" Width="30" Binding="{Binding Path=id}"/>
            <DataGridTextColumn Header="DESCRIPTION" Width="300" Binding="{Binding Path=description}"/>
            <DataGridTextColumn x:Name="amountColumn" Header="AMOUNT" Width="100" Binding="{Binding Path=amounts}"/>
            <DataGridTextColumn Header="CATEGORY" Width="280" Binding="{Binding Path=category}"/>
        </DataGrid.Columns>
    </DataGrid>
</Page>
